# Simple test configuration for Phase 1
# Actual tests will be added in later phases

enable_testing()

# Add test executables here as needed
# add_executable(test_cache test_cache.c)
# target_link_libraries(test_cache hamclock)
# add_test(NAME cache_test COMMAND test_cache)
